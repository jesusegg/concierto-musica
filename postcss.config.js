module.exports = {
    plugins: {
        "autoprefixer":true
    }
}
// para que funcione postcss hay que crear esta hoja de configuracion con el nombre exacto y exportar los modulos como autoprefixer(este plugin adapta la hoja de estilo automaticamente con versiones mas antiguas para mayor soporte super util)